using UnityEngine;

public class PatientScript : MonoBehaviour
{




    [SerializeField]
    PatientWoundScript patientWoundScript;

    public PatientWoundScript PatientWoundScript { get => patientWoundScript;}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
